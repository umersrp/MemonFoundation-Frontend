name: Deploy Frontend to EC2 with Docker (Main Only)
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.EC2_SSH_KEY }}
      
      - name: Deploy React Frontend to EC2 with Docker
        env:
          EC2_HOST_MAIN: ${{ secrets.EC2_HOST_MAIN }}
        run: |
          echo "Connecting to EC2 and deploying MemonFoundation-Frontend with Docker..."
          ssh -o StrictHostKeyChecking=no ec2-user@$EC2_HOST_MAIN << 'EOF'
            set -ex
            echo "Navigating to frontend directory..."
            cd /repos/MemonFoundation-Frontend
            
            echo "Pulling latest changes..."
            git remote set-url origin git@github.com:umersrp/MemonFoundation-Frontend.git
            git fetch origin
            git reset --hard origin/main
            
            echo "Stopping and removing existing container..."
            docker stop memonfrontend 2>/dev/null || true
            docker rm memonfrontend 2>/dev/null || true
            
            echo "Building Docker image..."
            docker build -t memonfrontend:latest --no-cache .
            
            echo "Starting Docker container..."
            docker run -d \
              --name memonfrontend \
              --restart always \
              -p 3001:80 \
              -e NODE_ENV=production \
              memonfrontend:latest
            
            echo "Cleaning up unused Docker images..."
            docker image prune -f
            
            echo "Checking container status..."
            docker ps | grep memonfrontend
            
            echo "Showing container logs (last 20 lines)..."
            docker logs --tail 20 memonfrontend
            
            echo "✅ Frontend deployment with Docker completed successfully!"
          EOF
